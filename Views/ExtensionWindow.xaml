<Window x:Class="TagNamer.Views.ExtensionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:vm="clr-namespace:TagNamer.ViewModels"
        mc:Ignorable="d"
        Title="확장자 변경"
        Height="220" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ui:WindowHelper.UseModernWindowStyle="True">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="변경할 확장자를 입력하세요."
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1"
                   Text="입력된 확장자로 일괄 변경됩니다. (예: jpg)"
                   Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                   FontSize="12"
                   Margin="0,0,0,15"/>

        <TextBox Grid.Row="2"
                 Text="{Binding ExtensionInput, UpdateSourceTrigger=PropertyChanged}"
                 ui:ControlHelper.PlaceholderText="확장자 입력 (점 생략 가능)"
                 VerticalAlignment="Top" />

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="변경"
                    Command="{Binding ApplyCommand}"
                    IsDefault="True"
                    Style="{StaticResource AccentButtonStyle}"
                    Margin="0,0,10,0" Width="80"/>
            <Button Content="취소"
                    Command="{Binding CancelCommand}"
                    IsCancel="True"
                    Width="80"/>
        </StackPanel>
    </Grid>
</Window>
